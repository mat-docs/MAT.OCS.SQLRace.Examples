using MAT.OCS.Core;
using MESL.SqlRace.Domain;
using MESL.SqlRace.Domain.Collections;

Core.LicenceProgramName = "SQLRace";
Core.Initialize();

Console.WriteLine("ItemEventArgs props:");
foreach (var p in typeof(ItemEventArgs).GetProperties())
    Console.WriteLine("  " + p.Name + " : " + p.PropertyType.Name);
Console.WriteLine();
Console.WriteLine("LapEventArgs props:");
foreach (var p in typeof(LapEventArgs).GetProperties())
    Console.WriteLine("  " + p.Name + " : " + p.PropertyType.Name);
Console.WriteLine();
Console.WriteLine("Lap props:");
foreach (var p in typeof(Lap).GetProperties())
    Console.WriteLine("  " + p.Name + " : " + p.PropertyType.Name);
Console.WriteLine();
Console.WriteLine("SessionEventArgs props:");
foreach (var p in typeof(SessionEventArgs).GetProperties())
    Console.WriteLine("  " + p.Name + " : " + p.PropertyType.Name);
